<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules for Android 12+ (API 31+).
    See https://developer.android.com/about/versions/12/backup-restore#xml-changes
-->
<data-extraction-rules>
    <cloud-backup>
        <!--
            Exclude the sync credentials SharedPreferences file from cloud
            backup. The file contains the plaintext sync server password
            (ondict_sync.xml). Backing it up to Google Drive would expose
            those credentials across device restores and account-sharing
            scenarios. Users are expected to re-enter their sync credentials
            on a new device; the Word Bank / history data itself is restored
            via the sync server, not via Google Backup.
        -->
        <exclude domain="sharedpref" path="ondict_sync.xml"/>
    </cloud-backup>
    <device-transfer>
        <!-- Same exclusion for local device-to-device transfers. -->
        <exclude domain="sharedpref" path="ondict_sync.xml"/>
    </device-transfer>
</data-extraction-rules>
